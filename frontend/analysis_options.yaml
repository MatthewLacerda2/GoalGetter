include: package:very_good_analysis/analyzer.yaml

analyzer:
  plugins:
    - custom_lint
  exclude:
    - "lib/**/*.g.dart"
    - "lib/**/*.freezed.dart"
    - "lib/l10n/generated/**"

linter:
  rules:
    public_member_api_docs: false
