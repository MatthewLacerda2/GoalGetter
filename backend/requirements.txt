# What backend/ imports, and nothing it merely happened to have installed.
# Exact pins on these; pip resolves everything underneath them (#163).
# A new line here means a new import under backend/ or a new gate tool.

# The API
fastapi==0.141.1
starlette==1.7.0
uvicorn==0.54.0
slowapi==0.1.10
httpx==0.28.1
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3

# Sign-in: Google's ID token, then our own JWT
google-auth[requests]==2.58.1
python-jose==3.5.0

# Database: async through asyncpg, alembic through psycopg2
sqlalchemy[asyncio]==2.1.1
asyncpg==0.31.0
psycopg2-binary==2.9.13
alembic==1.20.0
pgvector==0.5.0
numpy==2.5.3

# Gemini
google-genai==2.25.0

# The gates (make backend): they run inside this image
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
ruff==0.16.9
vulture==2.16
